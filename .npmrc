; pnpm configuration for spec-exam-expo

; Allow running install scripts for native build tools
; (esbuild, vitest binary, etc.)
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true

; Use the same resolution as npm/yarn for compatibility
resolve-peers-from-workspace-root=false
